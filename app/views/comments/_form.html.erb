<%= bootstrap_form_with model: [ @movie, @movie.comments.build ] do |form| %>
  <p>
   
    <%= form.text_field :commenter %>
  </p>
  <p>
    
    <%= form.text_area :content %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
